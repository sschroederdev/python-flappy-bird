# Keeps `pycodestyle .` and `python -m unittest discover` behaving the same
# locally as they do in CI. build/ holds pygbag output and .venv is not ours.
[pycodestyle]
exclude = .git,__pycache__,.venv,venv,virtualenv,build
